#include <kalman_tracker.hpp>

int main(int argc, char const *argv[])
{
    // auto img = imread("/home/lbstr/Pictures/image_2019-10-23_01-31-06.png");
    // imshow("show", img);
    // waitKey(0); 
    // testHist(img);
    int classId;
    float confidence;
    Rect bbox;
    // int classId;
    // float confidence;
    // Rect bbox;
    // bool registered =  false;
    struct A { int x; int z; int a = 0; };
    // A a{.y = 2, .x = 1}; // error; designator order does not match declaration order
    return 0;
}
